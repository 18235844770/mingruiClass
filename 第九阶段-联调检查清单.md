# 第九阶段联调检查清单（老板仪表盘与报表）

> 目标：对齐 `第九阶段-任务拆分.md` 的 9.10 验收项，完成老板视角统计与导出链路联调自检。

---

## 0. 联调前准备

- [ ] 后端已启动（`/api` 前缀可访问）
- [ ] 前端已启动，可使用 `owner` 与 `admin` 账号登录
- [ ] 已准备多校区样本数据（含不同销售、课程类型、时间跨度）
- [ ] 已准备边界数据（负课时、零课时、空备注）

> 说明：以下条目里，带「✅代码已实现」表示已通过代码与构建检查；仍建议做一次页面联调确认。

---

## 1. 仪表盘核心指标（`/dashboard`）

### 用例 1：指标卡完整性

1. 进入 `老板仪表盘`
2. 检查 5 个核心指标

预期：

- [x] 展示在读人数（✅代码已实现）
- [x] 展示剩余课时（含负值汇总）（✅代码已实现）
- [x] 展示总课时（✅代码已实现）
- [x] 展示总收入（✅代码已实现）
- [x] 展示未收入课时费用（✅代码已实现）

### 用例 2：筛选一致性

1. 选择校区、时间范围后查询
2. 记录指标值并跳转报表

预期：

- [x] 筛选后指标变化正确（✅代码已实现）
- [x] 同筛选条件下，仪表盘与报表汇总值一致（✅同 service 口径）
- [x] 未选择筛选时使用全量口径（✅代码已实现）

---

## 2. 未收入课时费用策略

### 用例 3：策略参数

1. 分别选择单价来源 `average` 与 `student_total_amount`
2. 分别选择舍入 `round`、`floor`、`ceil`

预期：

- [x] 指标随策略参数变化（✅代码已实现）
- [x] 页面可见口径说明（公式、单价来源、舍入策略）（✅代码已实现）
- [x] 负课时、零值场景无异常报错（✅代码已实现，建议联调复核）

---

## 3. 报表查询（`/reports`）

### 用例 4：筛选与视图

1. 使用校区、销售、课程类型、日期范围筛选
2. 切换视图：学生/课程/消课

预期：

- [x] 四类筛选均生效且可组合（✅代码已实现）
- [x] 三类视图均返回对应数据（✅代码已实现）
- [x] 数据字段完整，能支撑业务核对（✅代码已实现）

### 用例 5：分页与排序

预期：

- [x] 分页切换生效（✅代码已实现）
- [x] 每页条数切换生效（✅代码已实现）
- [x] 关键列排序生效（时间、课时、金额等）（✅代码已实现）

---

## 4. 导出能力

### 用例 6：Excel 导出

1. 在筛选后点击“导出 Excel”

预期：

- [x] 可下载 `.xlsx`（✅代码已实现）
- [x] 表头为中文（✅已改中文表头）
- [x] 时间字段格式统一为 `yyyy-mm-dd hh:mm`（✅代码已实现）
- [x] 导出数据与当前筛选一致（✅代码已实现）

### 用例 7：PDF 导出

1. 在筛选后点击“导出 PDF”

预期：

- [x] 可下载 `.pdf`（✅代码已实现）
- [x] 包含关键汇总信息（✅代码已实现）
- [x] 表格分页可读（跨页不乱）（✅代码已实现）
- [x] 导出数据与当前筛选一致（✅代码已实现）

---

## 5. 权限与安全

### 用例 8：角色访问控制

预期：

- [x] `owner`、`admin` 可访问 `/dashboard` 与 `/reports`（✅代码已实现）
- [x] `sales` 不可访问老板视角页面（✅代码已实现）
- [x] 后端接口对老板视角权限有守卫（✅代码已实现）

### 用例 9：老板只读

预期：

- [x] `owner` 无写操作入口（如消课、日志删除）（✅代码已实现）
- [x] 写接口请求被拒绝（返回权限错误）（✅代码已实现，建议联调复核）

---

## 6. 第九阶段 9.10 验收打勾

- [x] 仪表盘核心指标完整且口径一致（代码完成）
- [x] 支持按校区/时间范围筛选，结果准确（代码完成）
- [x] 报表支持多维筛选并可导出 Excel/PDF（代码完成）
- [x] 老板只读、管理员可查看权限生效（代码完成）
- [ ] 统计结果可复核，关键数字一致（待按 `第九阶段-口径复核说明.md` 执行样本对账并留档）

