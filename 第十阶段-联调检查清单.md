# 第十阶段联调检查清单（联调、测试、修复）

> 目标：对齐 `第十阶段-任务拆分.md` 的 10.1～10.10，完成上线前质量闭环联调。  
> 基线时间：2026-04-09

---

## 0. 联调前准备与基线

- [x] 后端构建通过（`backend npm run build`）
- [x] 后端单测通过（`backend npm run test`，3 suites / 8 tests）
- [x] 前端构建通过（`frontend npm run build`）
- [ ] 前后端联调环境固定（分支、数据库、环境变量）
- [ ] 准备联调样本数据（正常、边界、异常）

风险备注：

- 前端构建有 chunk 体积告警（主包较大），不阻断第十阶段联调，但建议在发布前评估拆包优化。

---

## 1. 字段与契约联调（10.1）

### 用例 1：DTO 与前端字段一致性

预期：

- [x] 字段名一致（含大小写）
- [x] 必填项一致
- [x] 枚举值一致
- [x] 默认值一致

### 用例 2：空值与边界语义

预期：

- [ ] `null` 与空串语义一致
- [ ] `0` 与负值处理一致
- [ ] 可空字段在页面与接口行为一致

### 用例 3：时间与金额格式

预期：

- [ ] 页面统一展示时间格式 `yyyy-mm-dd hh:mm`
- [ ] 金额精度一致
- [ ] 课时精度与负课时逻辑一致

---

## 2. 表单与错误提示检查（10.2）

### 用例 4：校验触发完整性

预期：

- [ ] 必填校验可触发
- [ ] 长度校验可触发
- [ ] 类型/范围校验可触发
- [ ] 重复校验可触发

### 用例 5：错误提示可读性

预期：

- [x] 后端错误 `message` 可透传（含数组 message 统一归一为可读文本）
- [x] 用户可理解并可操作
- [ ] 错误提示位置一致且醒目

### 用例 6：提交流程一致性

预期：

- [ ] 重复点击不会导致重复写入
- [ ] 新增/编辑/删除成功反馈统一
- [ ] 成功后列表刷新行为一致

---

## 3. 权限与数据隔离回归（10.3）

### 用例 7：角色权限

预期：

- [ ] `admin` 菜单与接口权限正确
- [ ] `sales` 菜单与接口权限正确
- [ ] `owner` 菜单与接口权限正确

### 用例 8：校区隔离与越权

预期：

- [ ] 销售仅可见当前校区且创建范围数据
- [ ] 伪造参数跨校区访问被拒绝
- [ ] 老板只读策略有效

---

## 4. 核心业务链路回归（10.4）

### 用例 9：学生链路

预期：

- [x] 新增学生可用（API smoke 已验证）
- [x] 课程录入可用（API smoke 已验证）
- [ ] 编辑学生可用
- [x] 删除学生可用（API smoke 已验证）

### 用例 10：消课与提醒链路

预期：

- [x] 消课登记后课时正确扣减（API smoke 已验证）
- [x] 负课时/低课时状态正确（API smoke 已验证低课时命中）
- [ ] 提醒列表与详情/消课跳转联动正确

### 用例 11：统计链路

预期：

- [x] 仪表盘与报表口径一致（API smoke 已验证）
- [x] 筛选条件一致时关键数字一致（样本复核已验证）

---

## 5. 日志与审计检查（10.5）

### 用例 12：写操作日志

预期：

- [ ] 用户/角色/校区写操作落日志
- [x] 学生/课程/消课写操作落日志（API smoke 已通过 `targetId` 追溯）
- [ ] `detail` 含关键上下文（对象 ID、前后值）

### 用例 13：日志追溯

预期：

- [ ] 可按模块检索
- [ ] 可按操作人检索
- [ ] 可按时间范围检索

---

## 6. 报表与导出复核（10.6）

### 用例 14：筛选准确性

预期：

- [ ] 校区筛选准确
- [ ] 销售筛选准确
- [ ] 课程类型筛选准确
- [ ] 日期范围筛选准确

### 用例 15：导出一致性

预期：

- [x] Excel 与页面筛选结果一致（汇总与明细顺序样本对账脚本已验证）
- [x] PDF 与页面筛选结果一致（样本 PDF 文本与接口上下文复核通过）
- [x] 样本数据可复核（`verify:reports-sample` 已完成 DB 对账）

---

## 7. 缺陷与回归闭环（10.7）

- [x] 已建立并持续维护 `第十阶段-缺陷修复记录.md`
- [ ] 每个缺陷完成复现、修复、自测、回归、关闭
- [ ] 上线前 P0/P1 清零

---

## 9. 本轮联调结果（2026-04-09）

- [x] 完成前后端契约静态对齐扫描（students/consumptions/users/roles/campuses/reports/dashboard/logs/auth）
- [x] 修复角色编辑接口字段契约不一致（前端不再提交 `name: null`）
- [x] 修复后端校验错误文案透传能力（前端统一支持数组 message）
- [x] 完成权限与隔离静态核对（前端路由角色与后端 `@Roles` 对齐；`CampusGuard` + service scope 含销售 `campusId + createdBy` 限制）
- [x] 新增并执行 API smoke 脚本：`backend/scripts/smoke-phase10.mjs`（登录→学生→课程→消课→低课时→仪表盘→报表→导出→日志追溯→清理）
- [x] 新增并执行报表样本对账脚本：`backend/scripts/verify-reports-sample.mjs`（DB 聚合 vs API summary，Excel 汇总 vs API summary）
- [x] 新增并执行 PDF 样本复核脚本：`backend/scripts/verify-reports-pdf-sample.mjs`（导出 PDF + 上下文快照比对）
- [ ] 待完成页面回归验证（角色编辑清空名称、表单校验提示文案）
- [x] 报表 PDF 与页面筛选一致性复核（样本完成）

---

## 10. 自动化增强（10.8）

- [x] 10.8.1 API 回归脚本：`npm run smoke:phase10`
- [x] 10.8.1 报表样本复核脚本：`npm run verify:reports-sample`
- [x] 10.8.1 PDF 样本复核脚本：`npm run verify:reports-pdf-sample`
- [ ] 10.8.2 单测补强
- [ ] 10.8.3 E2E 关键场景
- [ ] 10.8.4 CI 校验门禁

---

## 8. 第十阶段验收打勾（10.10）

- [ ] 关键业务流程全链路可用（学生、课程、消课、提醒、统计）
- [ ] 高优先级缺陷清零（P0/P1）
- [ ] 权限与校区隔离规则全面生效
- [ ] 日志追溯与报表导出能力可用
- [ ] 验收资料完整，可支持交付评审
