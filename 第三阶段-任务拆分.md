# 第三阶段：数据库建模与迁移 — 任务拆分（执行版）

> 依据：`开发流程.md` 第三阶段、`第一阶段-需求拆解与系统设计.md`、当前仓库 `backend/prisma/schema.prisma`。

**进度**：**3.1～3.3、3.4～3.8（文档与代码侧）已完成**。其中 **3.5「在目标 PostgreSQL 上执行 migrate 并自检」** 需你在本机/服务器配置 `DATABASE_URL` 后按 `第三阶段-数据库验证清单.md` 勾选；**3.8 中与「已成功 migrate」相关的项**依赖该环境步骤。

---

## 阶段目标（不变）

完成**正式** Prisma 数据模型、PostgreSQL 迁移、索引与种子数据，并输出 ER 说明，使第四阶段起的业务接口有稳定数据层。

---

## 3.1 模型范围与对齐（先做设计核对）

| 序号 | 任务 | 说明 |
| --- | --- | --- |
| 3.1.1 | 对照第一阶段表清单 | ✅ 已包含：`users`、`roles`、`campuses`、`user_campuses`、`students`、`student_courses`、`course_consumptions`、`operation_logs` |
| 3.1.2 | 统一主键策略 | ✅ 全库 UUID：`String @id @default(uuid())` |
| 3.1.3 | 统一时间字段 | ✅ 各表 `created_at` / `updated_at`（`@@map` snake_case） |
| 3.1.4 | 校区维度 | ✅ `students.campus_id`、`operation_logs.campus_id`（可空）；消课经 `student_course → student` 可推导校区，**未**在 `course_consumptions` 上冗余 `campus_id` |
| 3.1.5 | 创建人维度 | ✅ `students.created_by`、`course_consumptions.operator_id` → `users` |

**产出**：在 `schema.prisma` 顶部或单独备注中写 1 段「主键/时间/命名」约定说明（不必长文）。

---

## 3.2 逐表建模（Prisma Model）

按依赖顺序实现（先父后子）：

| 序号 | 任务 | 要点 |
| --- | --- | --- |
| 3.2.1 | `roles` | ✅ `code` 唯一、`name` 可选 |
| 3.2.2 | 调整 `users` | ✅ 已改为 `role_id` → `roles`，移除原 `roleCode` 字段 |
| 3.2.3 | `campuses` / `user_campuses` | ✅ `user_campuses` 已补 `updated_at` |
| 3.2.4 | `students` | ✅ 字段齐全；`@@unique([campus_id, name])` |
| 3.2.5 | `student_courses` | ✅；`@@index([studentId])` |
| 3.2.6 | `course_consumptions` | ✅；`student_course_id`、`consumption_time`、`operator_id` 索引 |
| 3.2.7 | `operation_logs` | ✅ `detail` 为 `Json`；`operator_id`、`campus_id`、`created_at` 索引 |

**产出**：更新后的 `schema.prisma`（含全部 `@@map` 与关系字段）。

---

## 3.3 索引与约束（性能与业务规则）

| 序号 | 任务 | 说明 |
| --- | --- | --- |
| 3.3.1 | 唯一约束 | ✅ 见 `第三阶段-ER说明.md`；`students` 同校区姓名使用 **部分唯一**（3.4 迁移） |
| 3.3.2 | 外键与级联策略 | ✅ 已写入 `第三阶段-ER说明.md`：学生删除 **CASCADE** 课程与消课记录；`users`/`campuses` 被引用处多为 **RESTRICT**；日志校区 **SET NULL** |
| 3.3.3 | 查询索引 | ✅ `students(campus_id, created_by)`、`course_consumptions` 时间/关联字段等 |

---

## 3.4 软删除策略（必须说明原因）

| 序号 | 任务 | 说明 |
| --- | --- | --- |
| 3.4.1 | `students` | ✅ 已增加 `deleted_at`；列表默认过滤未删除行 |
| 3.4.2 | `student_courses` | ✅ 已增加 `deleted_at`；与课程删除 UI 一致 |
| 3.4.3 | `operation_logs` | ✅ 已增加 `deleted_at`，对应「删除日志」隐藏行；**消课表不设软删**（见 `第三阶段-ER说明.md`） |
| 3.4.4 | 同校区重名 | ✅ 使用部分唯一索引 `WHERE deleted_at IS NULL`，迁移见 `20260402120000_soft_delete_and_partial_unique` |

**产出**：✅ `第三阶段-ER说明.md`「3.4 软删除策略」+ `schema.prisma` 字段注释。

---

## 3.5 迁移执行与验证

| 序号 | 任务 | 说明 |
| --- | --- | --- |
| 3.5.1 | 本地/测试库准备 | 见 **`第三阶段-数据库验证清单.md`** |
| 3.5.2 | 应用迁移 | `npx prisma migrate deploy` 或 `migrate dev`（两条迁移目录已提交） |
| 3.5.3 | 验证表结构 | 清单中的 Studio / `pg_indexes` 核对 |
| 3.5.4 | 与旧骨架衔接 | 清单中说明：旧库建议 **reset** 或空库 |

---

## 3.6 种子数据（Seed）

| 序号 | 任务 | 说明 |
| --- | --- | --- |
| 3.6.1 | 扩展 `prisma/seed.ts` | ✅ `roles` + `users.role_id` + `admin123` |
| 3.6.2 | 校区与用户绑定 | ✅ 示例校区 + `sales` 绑定 |
| 3.6.3 | 演示数据 | ✅ 「演示学生」「演示课程」+ 1 条消课（幂等） |
| 3.6.4 | 脚本入口 | ✅ `package.json` → `prisma.seed`（`tsx prisma/seed.ts`） |

---

## 3.7 ER 与文档交付

| 序号 | 任务 | 说明 |
| --- | --- | --- |
| 3.7.1 | `第三阶段-ER说明.md` | ✅ 含 Mermaid、软删除、部分唯一索引、迁移列表 |
| 3.7.2 | 关键字段对齐 | ✅ 见文档表格 |
| 3.7.3 | 关系图 | ✅ 已含 Mermaid `erDiagram` |

---

## 3.8 本阶段完成标准（自检清单）

- [x] `schema.prisma` 覆盖流程文档全部表，且**同校区学生姓名唯一（未删除行）**已体现  
- [ ] `migrate` 在目标 PostgreSQL 上**成功执行**（需你本地/服务器环境）  
- [ ] `prisma db seed` 可成功运行（需迁移已应用）  
- [x] ER 说明文档已提交，软删除与级联策略有文字说明（`第三阶段-ER说明.md`）  
- [x] 校区维度、创建人维度在表结构上可查询（有字段与索引支撑）

---

## 3.9 与第四阶段的衔接注意点

- 第三阶段**不**实现接口权限逻辑，但表结构应支持：`students(campus_id, created_by)` 供销售数据隔离查询。  
- JWT / 校区会话仍属第四阶段；本阶段只保证**库表就绪**。

---

## 建议执行顺序（1～2 个工作日可拆）

1. ~~定稿软删除与删除策略（3.4）~~  
2. ~~补全 `schema.prisma`（3.2）+ 索引（3.3）~~  
3. **在目标库执行** `migrate` + 按 `第三阶段-数据库验证清单.md` 自检（3.5）  
4. ~~扩展 seed（3.6）~~  
5. ~~ER 说明（3.7）+ 自检（3.8）文档~~
