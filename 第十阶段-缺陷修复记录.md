# 第十阶段缺陷修复记录

> 说明：第十阶段所有联调问题在此登记，按严重级别（P0/P1/P2）跟踪到关闭。  
> 状态流转：`待复现 -> 已复现 -> 修复中 -> 待回归 -> 已关闭`

---

## 0. 汇总看板

- 当前 P0：0
- 当前 P1：1
- 当前 P2：1
- 已关闭：0
- 未关闭：2

> 维护规则：新增一条缺陷后同步更新统计。

---

## 1. 缺陷明细

| 编号 | 严重级别 | 模块 | 问题描述 | 复现步骤 | 期望行为 | 实际行为 | 责任端 | 状态 | 修复方案 | 回归结果 | 记录时间 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| D10-001 | P1 | 角色管理 | 角色编辑提交 `name: null` 与后端 DTO 不一致 | 角色编辑时清空名称后保存 | 空名称应按“未提供字段”处理或给出清晰提示 | 请求体传 `null`，触发后端校验失败 | 前端 | 待回归 | 前端改为 `name: form.name.trim() || undefined` | 前端构建通过，待页面回归 | 2026-04-09 |
| D10-002 | P2 | 全局请求层 | 后端校验错误为数组时，前端提示文案无法直接透传 | 触发表单校验（如必填/长度）并观察 toast | 显示可读错误文案 | 部分页面拿不到可读 message | 前端 | 待回归 | 在 `http` 响应拦截器中统一归一 `message`（数组转字符串） | 前端构建通过，待页面回归 | 2026-04-09 |

---

## 2. 关闭标准

- P0/P1 缺陷在上线前必须全部 `已关闭`
- 每条缺陷必须有明确复现步骤与回归结果
- 修复后至少回归受影响功能与相邻主链路
